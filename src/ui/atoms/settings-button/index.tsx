import * as S from './styled';

const SettingsButton = () => {
  return (
    <S.Button
      whileHover={{ scale: 0.2 }}
      whileTap={{ scale: 0.75 }}
      onClick={() => {}}
    />
  )
}

export default SettingsButton;