import Button from "@/ui/atoms/button";

const Home = () => {
  return (
     <Button 
      label="Entre em Contato"
      fontSize={1.2}
      onClick={() => {}}
      variant="primary"
    />
  )
}

export default Home;